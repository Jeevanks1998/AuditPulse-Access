# --- Core framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Request/response validation ---
pydantic[email]==2.9.2

# --- Database (Postgres / Supabase) ---
psycopg2-binary==2.9.13

# --- AuditPulse integration (Phase 3) ---
httpx==0.27.2
python-dotenv==1.0.1

# --- Auth / MFA / SSO (Phase 5) ---
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (2020) predates bcrypt 4.1+'s removal of an internal
# attribute passlib's self-test relies on, which breaks hashing with a
# misleading "password cannot be longer than 72 bytes" error even for
# short passwords. Pinning bcrypt to the last version passlib 1.7.4 was
# actually tested against avoids that.
bcrypt==4.0.1
pyotp==2.9.0
